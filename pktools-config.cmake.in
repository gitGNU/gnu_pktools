# SET(PKTOOLS_LIBRARIES base imageClasses fileClasses algorithms)
SET(PKTOOLS_INCLUDE_DIRS @CMAKE_INSTALL_PREFIX@/include/pktools)
FIND_LIBRARY(BASE_LIB base
  PATHS @CMAKE_INSTALL_PREFIX@/lib
  # PATHS @CMAKE_BINARY_DIR
  NO_DEFAULT_PATH
)
IF(BASE_LIB)
  SET(PKTOOLS_LIBRARIES ${PKTOOLS_LIBRARIES} ${BASE_LIB})
ENDIF()
FIND_LIBRARY(ALGORITHMS_LIB algorithms
  PATHS @CMAKE_INSTALL_PREFIX@/lib
  # PATHS @CMAKE_BINARY_DIR
  NO_DEFAULT_PATH
)
IF(ALGORITHMS_LIB)
  SET(PKTOOLS_LIBRARIES ${PKTOOLS_LIBRARIES} ${ALGORITHMS_LIB})
ENDIF()
FIND_LIBRARY(FILECLASSES_LIB fileClasses
  PATHS @CMAKE_INSTALL_PREFIX@/lib
  # PATHS @CMAKE_BINARY_DIR
  NO_DEFAULT_PATH
)
IF(FILECLASSES_LIB)
  SET(PKTOOLS_LIBRARIES ${PKTOOLS_LIBRARIES} ${FILECLASSES_LIB})
ENDIF()
FIND_LIBRARY(IMAGECLASSES_LIB imageClasses
  PATHS @CMAKE_INSTALL_PREFIX@/lib
  # PATHS @CMAKE_BINARY_DIR
  NO_DEFAULT_PATH
)
IF(IMAGECLASSES_LIB)
  SET(PKTOOLS_LIBRARIES ${PKTOOLS_LIBRARIES} ${IMAGECLASSES_LIB})
ENDIF()
FIND_LIBRARY(LASCLASSES_LIB lasClasses
  PATHS @CMAKE_INSTALL_PREFIX@/lib
  # PATHS @CMAKE_BINARY_DIR
  NO_DEFAULT_PATH
)
IF(LASCLASSES_LIB)
  SET(PKTOOLS_LIBRARIES ${PKTOOLS_LIBRARIES} ${LASCLASSES_LIB})
ENDIF()

#     PATHS @CMAKE_INSTALL_PREFIX@/lib
#     NO_DEFAULT_PATH
# )
# SET(PKTOOLS_LIBRARIES ${PKTOOLS_LIBRARY})
# FIND_PATH(PKTOOLS_INCLUDE_DIR PKTOOLS.h
#     PATHS @CMAKE_INSTALL_PREFIX@/pktools/include/pktools
#     NO_DEFAULT_PATH
# )
# SET(PKTOOLS_INCLUDE_DIRS ${PKTOOLS_INCLUDE_DIR})
# # SET(PKTOOLS_DEFINITIONS ...)
# PKTOOLS_FOUND is set to "1" by FIND_PACKAGE().

# SET(PKTOOLS_INCLUDE_DIRS ${CMAKE_INSTALL_PREFIX}/include/pktools)
# SET(PKTOOLS_LIBRARIES ${PKTOOLS_BASE_LIB_NAME})
# SET(PKTOOLS_LIBRARIES ${CMAKE_INSTALL_PREFIX}/lib/libbase.so ${CMAKE_INSTALL_PREFIX}/lib/libalgorithms.so ${CMAKE_INSTALL_PREFIX}/lib/libimageClasses.so ${CMAKE_INSTALL_PREFIX}/lib/libfileClasses.so)

# SET(PKTOOLS_LIB_PATH ${CMAKE_INSTALL_PREFIX}/lib)
# SET(PKTOOLS_LIBRARIES ${CMAKE_INSTALL_PREFIX}/lib/libbase.so ${CMAKE_INSTALL_PREFIX}/lib/libalgorithms.so ${CMAKE_INSTALL_PREFIX}/lib/libimageClasses.so ${CMAKE_INSTALL_PREFIX}/lib/libfileClasses.so)
#this works
# SET(PKTOOLS_INCLUDE_DIRS ${CMAKE_INSTALL_PREFIX}/include/pktools)
# SET(PKTOOLS_LIB_PATH ${CMAKE_INSTALL_PREFIX}/lib)
# SET(PKTOOLS_LIBRARIES ${CMAKE_INSTALL_PREFIX}/lib/libbase.so ${CMAKE_INSTALL_PREFIX}/lib/libalgorithms.so ${CMAKE_INSTALL_PREFIX}/lib/libimageClasses.so ${CMAKE_INSTALL_PREFIX}/lib/libfileClasses.so)
#this does not work
# get_filename_component(SELF_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
# include(${SELF_DIR}/base-targets.cmake)
# include(${SELF_DIR}/algorithms-targets.cmake)
# include(${SELF_DIR}/imageClasses-targets.cmake)
# include(${SELF_DIR}/fileClasses-targets.cmake)
# # leave out for now...
# # include(${SELF_DIR}/lasclasses-targets.cmake)

# get_filename_component(PKTOOLS_INCLUDE_DIRS "${SELF_DIR}/../../include/pktools" ABSOLUTE)
